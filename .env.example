## GraphQL endpoint
# Full URL (optional). If omitted, UI will use a relative path under BASE_PATH.
# NEXT_PUBLIC_BASE_URL=http://localhost:8880/graphql

# Path prefix under which the UI is served (e.g., "/classic"). Leave empty for root.
NEXT_PUBLIC_BASE_PATH=

# GraphQL path relative to BASE_PATH. Defaults to "/graphql".
NEXT_PUBLIC_API_PATH=/graphql

## Socket.IO
# Socket origin (optional). If omitted, UI uses current window origin.
# NEXT_PUBLIC_SOCKET_ORIGIN=http://localhost:2096

# Socket path, defaults to `${BASE_PATH}/socket.io`.
NEXT_PUBLIC_SOCKET_PATH=/socket.io
