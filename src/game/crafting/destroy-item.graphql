mutation DestroyItem($item: ID!) {
  destroyItem(item: $item) {
    account {
      id
      nextAllowedAction
    }
    hero {
      id
      enchantingDust

      inventory {
        id
        baseItem
        owner
        name
        type
        level
        enchantment
      }
    }
  }
}
