mutation DisenchantItem($item: ID!) {
  disenchantItem(item: $item) {
    account {
      id
      nextAllowedAction
    }
    hero {
      id
      enchantingDust
      enchantments

      inventory {
        id
        enchantment
      }
    }
  }
}
